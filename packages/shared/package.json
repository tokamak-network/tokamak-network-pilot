{
  "name": "@tokamak-pilot/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, constants, and utilities for the Tokamak Pilot monorepo",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
